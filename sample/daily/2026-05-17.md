---
tags: [daily]
date: 2026-05-17
---
# 2026-05-17

## Done

- Merged PR #38 (property commands — `list`, `get`, `set`)
- Started property filter on `pumex search` — schema migration drafted
- Investigated System.CommandLine 2.0 breaking changes

## Blocker

System.CommandLine 2.0.8 changed how subcommands are registered. The `pumex note` subcommand group needs refactoring before new options can be added cleanly. The migration is bigger than a one-liner.

Splitting into a separate work item. See [[tasks/in-progress]].

## Notes

Key breaking change: `Command.AddAlias` is gone — now `Command.Aliases.Add`. Also, option binders changed. The `pumex note` group is the most impacted.

Property filter on search will wait until the migration lands — don't want to build on the old API.

## Tomorrow

- Focus on System.CommandLine 2.0.8 migration (unblocks shell completions too)
- Property search filter resumes after migration is stable
- Update [[wiki/daemon]] with any IPC changes that surface during migration
