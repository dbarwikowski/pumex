---
tags: [daily]
date: 2026-05-15
---
# 2026-05-15

## Done

- Started reading [[wiki/daemon]] to understand the watcher lifecycle and FTS5 delete-then-insert constraint
- Reviewed the SQLite schema in `IndexDb` — confirmed no UPDATE on FTS5 tables
- Added initial implementation of `pumex property list` command

## Notes

The FTS5 no-UPDATE rule is subtle. Added it to [[wiki/daemon]] so it is findable. Worth mentioning in code comments too.

The `WikilinkResolver` is per-vault — constructed by `IndexingServiceFactory`, never a DI singleton. Vault graphs would cross-pollinate if it were shared. Needs a test.

## Tomorrow

- Wire up `pumex property get` and `pumex property set`
- Write unit tests for property frontmatter round-trip
- Check [[tasks/backlog]] for anything else in the property area
