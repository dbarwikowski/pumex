---
tags: [daily]
date: 2026-05-18
---
# 2026-05-18

## Done

- Completed System.CommandLine 2.0.8 migration for `Pumex.Cli`
- All existing commands working with new API — regression tested manually
- Updated [[wiki/commands]] to reflect the new command surface
- Merged PR #39: System.CommandLine 2.0 migration

## Key changes in the migration

- `Command.AddAlias` → `Command.Aliases.Add`
- `Option<T>` constructor changed — `--` prefix no longer added automatically to some options
- `ParseResult.GetValueForOption` → `ParseResult.GetValue`
- Subcommand registration via `rootCommand.AddCommand` unchanged

## Notes

Shell completions are now unblocked. Will pick up from [[tasks/backlog]] once property search filter is shipped.

Confirmed `InvariantGlobalization=false` still correctly set in all project files — the migration didn't touch that.

## Tomorrow

- Resume property filter on `pumex search`
- Start scaffolding the sample vault — needs wiki pages for every command
- Update [[wiki/vaults]] with dev daemon isolation example
